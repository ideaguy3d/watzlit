{
  "rules": {
    ".read": true,
    "games": {
      "$gameId": {
        "metadata": {
          ".read": true
        },
        "gameContent": {
          ".read": "data.parent().child('tickets').hasChild(auth.uid)"
        },
        "tickets": {
          "$uid": {
            ".read": "auth.uid === $uid",
            ".write": "auth.uid === $uid"
          }
        }
      }
    }
  }
}